{
  "name": "slack-ext-mono-root",
  "private": true,
  "dependencies": {
    "@slack/interactive-messages": "^1.1.1",
    "better-error-message-for-json-parse": "^0.1.6",
    "decache": "^4.5.1",
    "handlebars": "^4.1.2",
    "slack": "^11.0.2",
    "ste-events": "^1.5.2"
  },
  "devDependencies": {
    "husky": "^2.4.1",
    "lerna": "^3.15.0"
  },
  "scripts": {
    "build": "lerna bootstrap & lerna run build",
    "test": "lerna bootstrap & lerna run test",
    "upgrade": "ncu -n & npm install & lerna run update-packages & lerna bootstrap & lerna run build & lerna run test & echo Upgrade(s) completed!",
    "make": "lerna bootstrap & lerna run test & lerna run build && lerna publish --yes --cd-version patch & git add . & git commit -m Version-bump && git push "
  },
  "husky": {
    "hooks": {
      "pre-push": "npm test"
    }
  }
}
